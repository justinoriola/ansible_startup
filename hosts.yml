---
datacenter:
  children:
    routers:
      hosts:
        192.168.1.253:
  vars:
    ansible_connection: network_cli
    ansible_network_os: cisco.ios.ios
    ansible_user: admin
    ansible_become_password: cisco1234
    ansible_ssh_pass: cisco1234
